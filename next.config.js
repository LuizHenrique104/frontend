/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // removed unrecognized option `swcMinify` for compatibility with this Next.js version
}

module.exports = nextConfig